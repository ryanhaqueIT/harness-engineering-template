[project]
name = "harness-demo-backend"
version = "0.1.0"
description = "Tiny demo backend to illustrate harness gate activation"
requires-python = ">=3.10"

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W"]
